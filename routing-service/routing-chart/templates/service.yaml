apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-routing-service
  labels:
    app: routing-service-{{ .Values.config.REGION_ID }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    app: routing-service-{{ .Values.config.REGION_ID }}